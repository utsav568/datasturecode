#include <bits/stdc++.h>
using namespace std;
int main() {
    set<int> s;
    s.insert(1);
     s.insert(3);
      s.insert(2);
       s.insert(9);
        s.insert(6);
        for(auto ele : s){
            cout<<ele<<" ";
        }
//log n  
}